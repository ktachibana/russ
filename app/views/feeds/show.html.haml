%h1
  = link_to @feed.title, @feed.link_url

= paginate @items

%ul
  - @items.each do |item|
    %li.item
      %h2.title= link_to item.title, item.link
      .description= simple_format item.description

= paginate @items
