= render 'tags'

= paginate @items

.items
  .panel.panel-default{ v: { repeat: 'items' } }
    .panel-heading
      %a{ target: '_blank', v: { attr: 'href:link' } }
        {{title}}
      .pull-right
        %small
          %a{ target: '_blank', v: { attr: 'href:feed.link_url' } }
            {{feed.users_subscription.user_title}}
          %a{ v: { attr: 'href:"/subscriptions/" + feed.users_subscription.id' } }
            ■
    .clearfix
    .panel-body{ v: { html: 'description' } }

= paginate @items
